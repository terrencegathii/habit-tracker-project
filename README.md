# Habit Tracker Project

This repository contains the structure for the habit tracker project. Use the `backend`, `frontend`, and `docs` directories for the server-side, client-side, and documentation files respectively.